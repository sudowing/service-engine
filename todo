

subquery
update logger with file and trace
make every http call get logged to debug

CRUD OPTIONS in service-config
relationships in service-config (for when fk is not a constraint)
unique vs primary key (applies to materialized views specificlly - but could have other value)

ALL VALIDATION needs to be async


  /resources accepts a get param for a single resource

  define relationships via config if fk not set
    fk defines itself (used for GraphQL)




add all supported types for postres -- ensure non breaking
https://www.postgresql.org/docs/current/datatype.html


`typecastFn`

8.1. Numeric Types
        smallint
        integer
        bigint

        decimal
        numeric
        real
        double precision
            * have special values (Infinity, -Infinity, NaN)

        smallserial
        serial
        bigserial

            int2
            int4
            int8

8.2. Monetary Types
      money || bigint in js
8.3. Character Types
      character varying(n)
      varchar(n)
      character(n)
      char(n)
      text
      "char"
      name


8.4. Binary Data Types
    bytea || string hexidecimal string

8.5. Date/Time Types
    timestamp
    timestamp
    date || this is literal full
    time
    time
    interval
    *string || all start with

8.6. Boolean Type
      boolean
8.7. Enumerated Types
      CUSTOM::string

8.8. Geometric Types
      point
      line
      lseg
      box
      path
      path
      polygon
      circle
        *string // get typed to geoJson on output

8.9. Network Address Types
        cidr
        inet
        macaddr
          * string

8.10. Bit String Types
      bit(n)
      bit varying(n)
        *string
8.11. Text Search Types

    8.11.1. tsvector
    8.11.2. tsquery

8.12. UUID Type
    uuid
    string
8.13. XML Type
    xml
    string

8.14. JSON Types
    json
    jsonb
    jsonpath
    *string

8.15. Arrays
  * ignore. catch all string will address.
  in the future -- breaking change will type

8.16. Composite Types
  * ignore. catch all string will address.

8.17. Range Types
      int4range
      int8range
      * int
      numrange
      * float
      tsrange
      tstzrange
      daterange
      * string

8.18. Domain Types
      CUSTOM:STRING
8.19. Object Identifier Types
      oid
          * int
      regproc
      regprocedure
      regoper
      regoperator
      regclass
      regtype
      regrole
      regnamespace
      regconfig
      regdictionary
          * string


8.20. pg_lsn Type
      pg_lsn
        * string

add graphql schemas
add support for mysql (count object will look different)
add support for sqlite
add support for oracle