
uuid in header
sql in header
count in header for search

logloglogs

create (seperate validators [pks and timestamps may be autogenerated])
    [batch] -- returning fields
read (normal validators)
    /search
    /record?pk
update
delete (soft delete only) (uses read PKs)

swagger docs

update logger with file and trace
make every http call get logged to debug
